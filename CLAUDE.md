# CLAUDE.md — Project Instructions

This file is read automatically at the start of every Claude Code session in this repo. It is the working contract for how this project gets built. Full context and rationale live in `PROJECT_PLAN.md` at the repo root — read it once at project start and whenever a phase changes; this file is the condensed, load-bearing summary you should follow every session.

## What this project is

A multi-tenant e-commerce SaaS platform for the Bangladesh market — merchants sign up, each gets their own online store (subdomain or custom domain), storefront, and admin dashboard, with (over time) AI-assisted tools. Modeled architecturally on a competitor audit (BitCommerz), not on its brand — see "Do not copy" below.

**Stack:** Next.js 16 (Turbopack, App Router), Postgres, Redis, Docker Compose, deployed on a single Hostinger VPS (Ubuntu 24.04, 2 vCPU / 8 GB RAM) behind Caddy \+ Cloudflare. Object storage on Cloudflare R2. Bengali \+ English UI (persisted toggle).

## Non-negotiable rules — check every change against these

1. **Every tenant-scoped table has a `store_id`, and every query is scoped by it.** No query against a tenant-scoped table skips this filter, ever. If you're writing a query and can't immediately point to the `store_id` filter, stop and fix it before moving on.  
2. **Postgres Row-Level Security is on for every tenant-scoped table**, as a second layer under the app-layer scoping above. When you add a new tenant-scoped table, add its RLS policy in the same migration — never as a follow-up.  
3. **Two billing systems, never merged in code:** `Order`/`Payment`/`Invoice` \= a customer paying a merchant. `Store.subscription`/`PlatformInvoice` \= a merchant paying the platform. Keep the naming and the modules separate.  
4. **Every marketing number, price, and feature claim lives in exactly one place** (a constant, a CMS field, a DB row) and every page that shows it reads from that one place. Never hand-write the same figure into two components — this was the single biggest bug class found in the competitor audit.  
5. **External integrations (payment, courier, SMS) go behind one internal adapter interface per concern**, with one implementation per provider. The app calls the interface, never a specific provider's SDK directly, outside that provider's own adapter file.  
6. **No fragile scroll-jacked/IntersectionObserver-driven carousels for anything load-bearing** (feature showcases, product carousels). Use a tested library or a simple paginated/tabbed layout instead.  
7. **Never render raw internal identifiers, category paths, or enum values as user-facing labels.** Always map to a clean display string.  
8. **Any count or claim shown to users (review count, "X merchants," etc.) must be either live/accurate or absent.** Don't hardcode an aspirational number.  
9. **Filter out demo/seed/test data from anything a real customer or merchant can see in production.** Add an `is_demo` flag if you need test fixtures that resemble real data.  
10. **Secrets never get committed.** `.env` is gitignored from the first commit.

## Do not copy

Don't reuse BitCommerz's name, logo, exact color hex values as "the brand," or any of its marketing copy verbatim — those belong to Bit Byte Technology Ltd. Building similar *functionality* is fine and is the whole point of this project; copying their specific brand identity is not, and also isn't useful once we have our own name. Ask the user for the project's real name/branding if it isn't decided yet, and use a clearly generic placeholder (e.g. "Platform") in code and copy until it is.

## Out of scope — do not build

Explicitly excluded by the user, even though the audited competitor has them:

- **POS (point-of-sale) system.** No in-person register/checkout mode, no card-reader/hardware integration, no "sell in-store" flow of any kind.  
- **Physical store / shop-location management.** No store-locator, no walk-in shop address/hours/map/360°-tour fields, no public-facing "visit us" features.

Don't add these back "for completeness" — if a later phase seems to need them, ask first.

- **Multi-warehouse stock.** Also cut, by the user's decision on 2026-08-31 (it was in scope until then — `PROJECT_PLAN.md` §8's original Phase 4). Inventory is a single scalar `product_variants.quantity` per variant, one implicit location. No `warehouses` / `warehouse_stock` tables, no per-location stock, no allocation logic, no Warehouses admin page. Don't reintroduce it without asking.  
- **Webhooks (outbound signed event push).** Cut from the active roadmap by the user's decision on 2026-09-01 — the user wants integrations to happen on-site, not pushed out to external endpoints, at least for now. Unlike the two cuts above, this one was already fully built and shipped (Phase 6 slices 2b + 2d: migration 0031, `src/lib/webhooks`, the admin Webhooks screen, `order.created`/`order.status_changed`/`order.paid` dispatch calls) — the code is **not** being reverted, it stays in the repo exactly as-is. What's cut is *further work*: no retry-queue, no endpoint auto-disable, no secret rotation, no new events, no promoting it in marketing/docs. Don't build on it or advertise it without asking; revisit only if a real external-integration need comes up.

## Current phase

Track progress here — update this section as phases complete. Start here:

- [x] **Phase 0 — Foundation (local dev complete):** Next.js 16 scaffold, Postgres \+ RLS (verified: `amarshop_app` role genuinely bound by RLS, migration role bypasses as expected), Redis, Docker Compose, staff auth with roles (Auth.js, owner/admin/staff \+ platform-admin flag), store creation flow, `proxy.ts` host-based tenant resolution — all built and tested against the local Docker stack.  
  - [ ] **Deferred — needs a real VPS:** server hardening (UFW/fail2ban/unattended-upgrades); the GitHub Actions CI/CD deploy pipeline; and activating custom-domain TLS — point docker-compose's `caddy` service at `Caddyfile.production` (on-demand TLS + the `/api/internal/domain-check` ask endpoint), set `PLATFORM_PUBLIC_IP`, and for real `*.PLATFORM_ROOT_DOMAIN` wildcard TLS rebuild Caddy with a DNS-01 provider plugin via xcaddy. The custom-domain admin flow + DNS verification are already built and testable locally with `CUSTOM_DOMAIN_VERIFY_MODE=trust`. Come back to these the moment a VPS is provisioned, before real users touch it — don't let "we're on Phase 1 now" become a reason to skip them later.  
- [x] **Phase 1 — Core commerce MVP (local dev complete):** storefront (home/category/product/cart/checkout/search), guest checkout with COD \+ SSLCommerz (IPN \+ Order Validation, per-store credentials from the admin), manual order entry and the full status pipeline, all three courier adapters behind one interface (credentials per store), auto-generated invoice PDFs, guest order tracking (`/track`), custom domains (admin flow \+ DNS verification \+ Caddy on-demand-TLS ask endpoint), admin shell \+ dashboard, Bengali/English toggle across storefront and admin. Closed out with a QA pass against `PROJECT_PLAN.md` Section 2's bug list — see the "Phase 1 closeout" commit.  
  - [ ] **Not yet exercised against real providers:** SSLCommerz, REDX/Pathao/Steadfast are built to their published contracts but no live merchant account exists yet. Run one real sandbox transaction per provider before any real customer does.  
- [x] **Phase 2 — Growth features (local dev complete):** discount/coupon codes (percent / fixed / free-delivery, total + per-phone limits); staff management UI + own-password change; bulk CSV product import; merchant Meta Pixel / GA4 storefront tags; low-stock alerts in the admin bell (per-store threshold); SMS adapter (BulkSMSBD + `log` dev mode) behind one interface, with order-placed / order-shipped notifications via `after()`; incomplete-checkout leads (debounced capture at checkout, manual phone follow-up, converted-on-order); blog / CMS (one `content_entries` table for posts + static pages, markdown via `marked` + `sanitize-html`, `/blog` + `/pages/[slug]` + footer links + the app's first `generateMetadata`); manual bKash / Nagad payment behind the `PaymentAdapter` interface (per-store numbers, customer enters TrxID, merchant marks paid). Closed out with a QA pass against `PROJECT_PLAN.md` Section 2's bug list — see the "Phase 2 closeout" commit.  
  - [ ] **Not yet exercised against real providers:** BulkSMSBD and the manual-wallet flow are built to their published contracts / the merchant's own instructions but no live account has sent a real message / verified a real payment. Plus the Phase 1 carry-over: SSLCommerz + REDX/Pathao/Steadfast still need one real sandbox transaction each.  
- [x] **Phase 3 — AI features (local dev complete):** AI product-description writer and AI SEO writer (title + meta), both behind one provider-agnostic `CopywriterAdapter` (`src/lib/ai`) — OpenAI-compatible / Anthropic / an offline stub that is the default with no key; `products.seoTitle`/`seoDescription` + the storefront PDP's first `generateMetadata`. COD fraud check via the BDCourier API (`src/lib/fraud`, one platform token, offline stub default) — auto on every COD order via `after()`, advisory badge on the order list + a panel with a re-check button on the detail. Demand-forecast v1 — a 30-day sales-velocity heuristic (`src/lib/products/forecast.ts`) projecting days-of-stock-left, shown as a products-list column and a dashboard "Restock Soon" panel. Closed out with a QA pass against `PROJECT_PLAN.md` Section 2's bug list — see the "Phase 3 closeout" commit.  
  - [ ] **Deliberately not built:** abandoned-cart SMS automation from `PROJECT_PLAN.md` §8 — the user reshaped abandoned-cart into manual phone follow-up (see the incomplete-checkout-leads slice, Phase 2), so there is no automated customer SMS. Don't add it back without asking.  
  - [ ] **Not yet exercised against real providers:** the AI writer (no `AI_API_KEY` set — runs on the stub) and BDCourier (`BDCOURIER_API_KEY` unset — stub). Run one real call against each before relying on the output.  
- [x] **Phase 4 — Digital products (local dev complete):** digital selling is a store-level capability picked on the create-store form (`stores.digitalEnabled`) — a digital store may sell digital + physical, a plain e-commerce store never sees the option. A digital product carries one or more **PDF files** (`product_digital_files`, validated on type + `%PDF-` magic bytes + 50 MB / 10-file caps, stored via the storage adapter); no stock, no shipping. The buyer downloads via an authenticated route `/order/[tranId]/download/[fileId]`, guest-gated by the checkout `tranId` and released only once payment is `paid` (or the order also has a physical line). An all-digital cart skips the delivery step + charge and hides COD; `createOrderRecords` takes a nullable `deliveryZoneId` and skips the stock decrement for digital lines. Digital products are excluded from low-stock alerts, forecasts and the dashboard count. Multi-warehouse — the original other half of this phase — was cut on 2026-08-31 (see "Out of scope"). Closed out with a QA pass against `PROJECT_PLAN.md` Section 2's bug list — see the "Phase 4 closeout" commit.  
  - [ ] **Not built:** per-order unique keys / license pools, download-count limits or link expiry, emailed download links, a repeat-buyer library, non-PDF digital goods, an R2 storage backend (still `LocalStorageAdapter`).  
- [x] **Phase 5 — Platform billing / business layer (local dev complete):** the merchant-pays-AmarShop system, kept in its own module (`src/lib/billing`, `src/lib/platform`) with names (`Plan` / `Subscription` / `PlatformInvoice` / `platform_invoices`) never merged into the customer-facing `Order`/`Payment`/`Invoice` (rule #3). Plan tiers are **one constants file** (`src/lib/billing/plans.ts`) — Free ৳0 / Starter ৳1000 / Business ৳1500, all self-serve; every screen + the receipt PDF reads price/limits from it. `platform_invoices` and the `stores.subscription_*` columns sit **outside** the `app.current_store_id` RLS boundary (like `stores`), reached only by explicit `where store_id = ?` — the migrations spell out why this is not a rule #2 gap. Slices: **v1** — plan picker + manual bKash/Nagad flow (per-store, merchant reports TrxID, platform admin verifies) + `/billing` admin page + a minimal `/platform/billing` verify list. **Limit enforcement** — catalog/staff caps blocked on create (`checkPlanLimit`); a monthly **order quota** (calendar month, Asia/Dhaka) where over-cap orders are still recorded but redacted in the admin (count + code/date only on `/orders/locked`) until upgrade — a SECURITY DEFINER `platform_store_stats()` is the sanctioned cross-tenant read. **Platform-admin dashboard** (`/platform`) — every store with MRR / GMV / order rollups, filters, suspend/activate + inline plan override; **store detail** (`/platform/stores/[id]`) — owner+staff (with the new `staff_members.phone`), commerce counts, subscription + invoice history, editable store settings, slug-confirmed hard delete. **Billing v2 lifecycle** — `runBillingLifecycle()` (lapsed trial → canceled, lapsed period → past_due, 3-day grace → suspended) run hourly by a `billing-cron` compose service via `POST /api/internal/billing/tick` (Bearer `BILLING_TICK_SECRET`), plus a read-time self-heal in `getSubscription`; `applyPaidPlan` reactivates a non-payment suspension; PDF receipts for paid invoices. Also folded in: `proxy.ts`/`tenant/resolve.ts` now strip the Host `:port` (so `http://localhost` and `:3000` both resolve); Auth.js `trustHost: true`; `requirePlatformAdminPage()` redirects unauth `/platform/*` to `/login` instead of a raw error. Closed out with a QA pass against `PROJECT_PLAN.md` Section 2's bug list — see the "Phase 5 closeout" commit.  
  - [ ] **Enterprise tier removed by the user (2026-08-31):** the plan grid is Free/Starter/Business only; there is **no** self-serve Enterprise tier and no sales-lead funnel was built. `PROJECT_PLAN.md` §8's "SME/Startup/Business/Enterprise" naming and its "Enterprise sales-lead funnel" line are stale — don't build the funnel back without asking.  
  - [ ] **Needs infra / not exercised for real:** `BILLING_TICK_SECRET` is only set in local `.env` (unset → the tick endpoint 503s and no automation runs) — set it wherever this deploys. No real merchant has completed a subscription payment; the manual bKash/Nagad platform-billing flow is built to the same contract as the storefront manual-wallet flow but unverified live. ShurjoPay / any recurring-payment aggregator (`PROJECT_PLAN.md` §5) was **not** integrated — platform billing is manual-only.  
- [ ] Phase 6 — Ecosystem (apps/developer platform) (**current**) — built in slices, on the shared credential substrate below:  
  - [x] **Slice 1 — public read API + scoped per-store API keys:** `api_keys` (migration 0029, SHA-256 hash + `ak_` prefix + comma-joined `scopes`, deliberately outside RLS like `stores`). `src/lib/api` — `scopes.ts` (`API_SCOPES` = `read:products`, `read:orders`, the single source), `keys.ts`, `http.ts` (`authenticateApi`: Bearer + 120/min per-key rate limit + scope check + `{ data, page, limit, hasMore }` / `{ error: { code, message } }` envelope), `dto.ts` (stable public shapes). `/api/v1` GET `/products`, `/products/[id]`, `/orders`, `/orders/[id]` — paged, scope-guarded, `withStoreContext`, quota-locked orders excluded. Admin → API Keys (mint/list/revoke). `proxy.ts` skips `/api/v1/*`. `docs/API.md`. See the "Phase 6 slice 1" commit.  
  - [x] **Slice 2a — OAuth app-install flow (developer platform):** OAuth 2.0 authorization-code flow layered on slice 1's scopes. Migration 0030 adds `oauth_apps` (platform-global registry — client_id/secret, redirect-URI allowlist, max scopes, status), `app_installations` (per-store install + its `ato_…` access token), `oauth_authorization_codes` (10-min single-use, optional PKCE S256) — **all three outside the RLS boundary, same chicken-and-egg justification as `api_keys`** (the migration + schema comments spell it out); `app_installations` is still store-scoped by explicit `where store_id = ?` everywhere. `src/lib/oauth` — `apps.ts` (CRUD + secret hash/verify + redirect-URI parse/exact-match), `flow.ts` (`issueAuthorizationCode` / `exchangeAuthorizationCode` — confidential clients: `client_secret` always required, PKCE enforced when a challenge was sent; re-install supersedes the prior token), `install.ts` (`resolveInstallationToken` — the `ato_` pre-context Bearer lookup, mirrors `resolveApiKey`, joins `oauth_apps` so a disabled app kills every token). `/oauth/authorize` (consent screen — owner/admin only, renders an error page until `redirect_uri` is allowlisted, never bounces an unverified URI) + `/oauth/token` (RFC 6749 envelope, form or JSON, HTTP-Basic client auth). `/api/v1` `authenticateApi` now accepts `ak_` **or** `ato_` (unchanged routes). Admin → Installed Apps (list + uninstall). `/platform/apps` (register/edit, regenerate secret, disable/enable + install count). `login` gained a validated `?next=` deep-link (used by the logged-out consent bounce). `proxy.ts` also skips `/oauth/*`. `docs/API.md` has an OAuth section. Verified end-to-end against the Docker stack (token exchange, single-use codes, secret/redirect binding, scope enforcement, disable-revokes, re-install-supersedes).  
  - [x] **Slice 2b — webhooks (signed event push):** migration 0031 adds `webhook_endpoints` (per-store `url` + retrievable `encryptSecret()`'d signing secret + comma-joined `events` + `disabledAt`) and `webhook_deliveries` (`payload` + `status` `webhook_delivery_status` = `success`/`failed` + `attempts` + `responseStatus`/`responseBody`/`error`) — **both ordinary RLS tables** (no pre-store-context lookup, unlike `api_keys`/`oauth_*`). `src/lib/webhooks` — `events.ts` (`WEBHOOK_EVENTS` = `order.created`, `order.status_changed` — the single source), `endpoints.ts` (CRUD, secret revealed decrypted in the admin), `dispatch.ts` (`emitWebhook` — never throws, mirrors `sendOrderSms`; snapshots the order as the same `orderToDto` the REST API returns; quota-locked orders fire nothing; **3 inline attempts at 0/2s/8s inside `after()`**, then a `failed` row; `resendDelivery` replays the stored payload, attempts accumulate). Headers `X-AmarShop-Signature: sha256=<hmac>` / `-Event` / `-Delivery`. Fired via `after()` from `checkout` `placeOrder`, `orders` `createManualOrder` (`order.created`) and `advanceOrderStatus` / `cancelOrder` (`order.status_changed`). Admin → Webhooks (add/reveal-secret/enable-disable/delete + a recent-deliveries log with Resend). `docs/API.md` has a Webhooks section. **No** cron / async retry queue, no endpoint auto-disable, no secret-rotation flow (delete + re-add). (`order.paid` added in slice 2d.)  
  - [x] **Slice 2c-i — OAuth app logos:** `src/lib/images/optimize.ts` — `optimizeImage(buf, maxEdge=2048)` factored out of `products/media.ts` (still re-exported there) so non-product callers don't pull the ffmpeg/drizzle path. `src/lib/oauth/logo.ts` — `setOAuthAppLogo` / `removeOAuthAppLogo` / `validateLogoFile` (PNG/JPEG/WebP ≤ 2 MB), stored via the storage adapter at `oauth-apps/<appId>/logo-<uuid>.webp` (256² webp), URL kept on the existing `oauth_apps.logo_url` (no migration). `/platform/apps` create+edit forms take a logo file (+ Remove logo); the logo shows on the `/oauth/authorize` consent screen, the merchant's Installed Apps list, and the `/platform/apps` list. No new i18n namespace — keys added under `platform.apps.*`.  
  - [x] **Slice 2d — `/api/v1` write surface + `order.paid` webhook:** scopes gained `write:products` / `write:orders` (auto-flow into the API-keys form, `/platform/apps` picker, `/oauth/authorize`). New: `PATCH /orders/{id}` (`status` = admin-style next-step / `canceled` → else `409 invalid_transition`; `payment:"paid"` on a non-gateway order → else `409 payment_gateway_managed`; validated-then-applied, no partial write), `POST /orders` (COD-only, mirrors manual entry, counts against the order quota), `PATCH /products/{id}` (status/name/brand/description, slug frozen), `PATCH /variants/{id}` (quantity/price/discountedPrice; `quantity` on digital → `409 not_applicable`; returns the parent `ProductDto`), `POST /products` (physical only, `403 plan_limit` at the cap, `409 sku_taken`). `src/lib/orders/{status,mutate}.ts` — `nextOrderStatus` + `advanceOrderStatusTx` / `cancelOrderTx` / `markOrderPaidTx`, shared verbatim by the admin order actions (rewritten to call them) and the routes; `(admin)/orders/status-pipeline.ts` is now a thin re-export. `src/lib/api/{validate,records}.ts` — field validators + `loadOrderDto`/`loadProductDto` (GET routes refactored onto them). `order.paid` added to `WEBHOOK_EVENTS`, fired once per order from every settle path: `markPaymentReceived`, a born-paid `createManualOrder`, `confirmSslcommerzPayment` (now returns `{result, orderId, firstPaid}`; the IPN + return routes emit only on `firstPaid`), and `PATCH /orders`. No migration. `docs/API.md` gains a Writing section + the `order.paid` row.  
  - [ ] **Webhooks deprioritized (2026-09-01):** cut from the active roadmap — see "Out of scope" above. The shipped slice 2b/2d code stays as-is; no further webhook work (retry queue, endpoint auto-disable, secret rotation, new events) is planned.
  - [ ] **Slice 2c-ii (next):** install analytics on `/platform`; re-consent when an app's scopes change; audit-log entries for install/uninstall/disable (the `audit_logs` table exists since Phase 0 but is still unused — no viewer UI yet); a public app directory.  
  - [ ] **Not exercised for real:** no third-party app exists yet; the `/oauth/authorize` consent UI, `/platform/apps` and `/installed-apps` screens, and the Webhooks admin + delivery path are built + locally tested (26-check host harness + Docker smoke) but unverified by a real external integration. Access tokens **do not expire** (revoked on uninstall / app-disable / re-install only) — no refresh-token grant; ShurjoPay-style recurring aggregators still not integrated.

Do not start work from a later phase before the current one is checked off, even if it looks quick — `PROJECT_PLAN.md` Section 8 explains why the order matters.

## Confirmed backlog (2026-09-01) — needed, not yet built

Reviewing `SITE_STRUCTURE.md` against actual build state surfaced a list of Part A/B pages that were never built and had fallen off active tracking. The user confirmed **all of them are still needed** — none are cut, they're simply not built yet. Grouped below by the phase `SITE_STRUCTURE.md` itself assigns each to; work through them in that order, same "don't skip ahead" rule as the phase table above.

- [x] **Phase 0/1 — Settings & shell gaps** (`SITE_STRUCTURE.md` Part B "Settings") — done 2026-09-01, Docker-build + live RBAC-enforcement verified:
  - [x] `Roles` (`/roles`) — a `PERMISSIONS` catalogue (`src/lib/auth/permissions.ts`, 15 resources, mirrors `API_SCOPES`/`WEBHOOK_EVENTS`), `custom_roles` (migration 0037, RLS) + `staff_members.custom_role_id`. Owner/admin keep unconditional access (unchanged); a "staff"-rank member gets nothing until assigned a custom role — `requirePermission()` (`src/lib/auth/roles.ts`) replaced `requireRole("admin")` at all 43 call sites that were admin-gated. Products/orders/categories stay open to every staff role, untouched. `/roles` (create/edit/delete + permission checkboxes), `/staff` gained a per-row custom-role picker, and the admin nav (`layout.tsx`) filters itself for a "staff" viewer via `NAV_PERMISSION`. Live-tested end-to-end: a staff member with only `support:manage` saw `/support` in nav and could load it, didn't see `/billing` in nav and got a 500 hitting it directly, and could still reach `/orders`/`/products`.
  - [x] General Settings tabs — `/account` is now a `?tab=` URL-synced shell: Profile (name/phone/bio — `staff_members.bio` new), Appearance (text-size only, applied sitewide via `admin-shell.tsx` + `src/lib/appearance/font-scale.ts`, localStorage), Notifications (per-staff `notify_billing_notices` toggle, filters the bell), Security (password only), Company (`stores.business_address`/`timezone`/`currency`), System (version, backup info, a real products CSV export at `/account/export/products`, a working `stores.maintenance_mode` toggle, and "Request store deletion" — owner-only, typed-name confirm, writes `audit_logs` (first real use of that table) + a critical notice; does **not** hard-delete — completion stays a platform-admin action at `/platform/stores/[id]`). **Deliberately not built:** 2FA, session timeout, login notifications, IP allowlist (Security); full color theme/dark mode/border radius (Appearance) — flagged in-page, not faked.
  - [x] Guest Checkout & OTP settings page (`/guest-checkout`) — `stores.checkout_otp_required` toggle, grayed out (client **and** server-checked) unless `store_sms_settings` has a real `bulksmsbd` provider configured. Settings-only, per the user's scope call — no OTP send/verify wired into checkout itself yet.
  - [x] `Locations` (`/locations`) — genuinely platform-wide, non-tenant `bd_divisions`/`bd_districts` tables (migration 0035, no RLS, no `store_id`), seeded in the migration itself with the real 8 divisions + 64 districts. Read-only browse page. Upazila-level data intentionally not included (needs a verified source first).
  - [x] Notices (`/notices`) + the admin bell wired to a real `notices` table (migration 0033, RLS) — `src/lib/notices`, categories `billing_trial_ended`/`billing_past_due`/`billing_suspended`/`store_deletion_requested`, emitted from `runBillingLifecycle()` and the deletion-request action. The bell (`admin-shell.tsx`) shows notices and stock alerts as two sections under one badge count, both from real queries.
  - [x] Support page (`/support`) — `stores.support_email`/`support_phone`/`support_hours` (migration 0032). Admin-side settings only; surfacing these on the storefront (footer/contact page) is a natural follow-up, not done yet.
  - **Bug caught during Docker verification, fixed same slice:** `src/app/page.tsx` (the platform-root/homepage URL — the one route that can't share `(storefront)/layout.tsx`, per its own comment) didn't check `maintenanceMode` at all, so toggling maintenance on left the real homepage fully visible. Fixed by extracting `<StorefrontMaintenance>` (`src/components/storefront-maintenance.tsx`) and checking it in both places. Verified live: toggling the DB flag now genuinely swaps the homepage for the maintenance page and back.
- [x] **Phase 1/2 — Online Shop / storefront customization** (`SITE_STRUCTURE.md` Part B "Online Shop") — done 2026-09-01, Docker-build + live-storefront verified:
  - [x] **Pages** — already fully covered by the existing `content_entries` (`kind: "page"`) + `/content` admin + `/pages/[slug]` route from Phase 2; nothing new built.
  - [x] Appearance (`/appearance`) — `stores.logo_url`/`primary_color` (logo upload reuses the OAuth-app-logo pattern: `optimizeImage` → storage adapter → URL on the row) + a Footer section (`footer_tagline`, `social_whatsapp`/`facebook`/`instagram`). `StorefrontHeader`/`StorefrontFooter` render them when set, unchanged when not.
  - [x] Menu Builder (`/menu-builder`) — `nav_menus` + `nav_menu_items` (migration 0039, RLS; items: custom_link/page/category, plain typed `display_order`, no drag-and-drop — no such precedent exists anywhere in this codebase). The admin UI get-or-creates one menu per store lazily (menu "status" isn't surfaced as its own step). `StorefrontHeader` uses it when present, falls back to the plain category list otherwise. **Bug caught live and fixed same slice:** nothing stopped two `is_active=true` menu rows existing per store, which made "the" active menu ambiguous and silently fell back to the wrong (empty) one — fixed with a partial unique index (`nav_menus_one_active_per_store_idx`, migration 0042).
  - [x] Shop Preview (`/shop-preview`) — an iframe of the real, live storefront URL with desktop/tablet/mobile width toggles; deliberately not a separate rendering path (the exact bug `SITE_STRUCTURE.md`'s audit flagged).
  - [x] Checkout Settings (`/checkout-settings`) — `checkout_custom_fields` + `order_custom_field_answers` (migration 0040, RLS; text/textarea only, no dropdown/checkbox yet). Rendered after the built-in Order Notes field; `placeOrder` re-validates required fields server-side and `createOrderRecords` persists a label+value snapshot per answer in the same order-creation transaction. Answers show on the admin order-detail page.
  - [x] Default Pages (`/default-pages`) — `stores.home_show_categories`/`home_categories_order`/`home_show_new_arrivals`/`home_new_arrivals_order`; toggles and reorders the homepage's two existing sections. Not a block/content editor — PDP/cart/checkout stay fixed layouts.
  - **Deliberately not built, flagged not faked:** typography/font picker (Bengali-glyph risk — an arbitrary Latin font can break Bengali rendering), a real drag-and-drop widget builder for headers/footers (no precedent in this codebase, folded into two plain fields instead), mobile bottom nav customization, checkout fields beyond text/textarea.
- [x] **Phase 2 — Content & Integrations** (`SITE_STRUCTURE.md` Part B "Content" / "Integrations") — done 2026-09-01, Docker-build + module-integration-smoke verified:
  - [x] Media Library (`/media`) — done 2026-09-01, Docker-build + host integration-smoke verified. `media_folders` + `media_assets` (migration 0043, both ordinary RLS tables). One flat level of folders, no nesting (no tree-UI precedent, same call as Menu Builder); deleting a folder keeps its files (FK `ON DELETE SET NULL` → they fall back to the library root). `src/lib/media` — `constants.ts` (client-safe caps: images JPEG/PNG/WebP ≤4 MB re-encoded to WebP via `optimizeImage`, PDF documents ≤25 MB stored as-is + `%PDF-` magic-byte check reusing the digital-products approach, ≤20 files/upload), `folders.ts`, `assets.ts` (validate/store/list/update/delete, all `withStoreContext`, keys `media/<storeId>/<uuid>.<ext>`). Serving: `/uploads/[...key]` route's content-type whitelist gained `pdf` (images already worked) — library files are public storefront content like product media; digital-product PDFs are unaffected (they never get a storage-adapter URL). Admin `/media` — `requirePermission("content:manage")` (Media Library is in `SITE_STRUCTURE.md`'s "Content" group next to Blog — reuses the existing permission, no new one minted; `NAV_PERMISSION["/media"]` set too), tabs All Files / Images / Documents / Folders, folder chips, multi-file upload, per-asset copy-link / alt-text / move-folder / two-step inline delete (no native `confirm()`). Nav entry after `/content`; `admin.media.*` + `admin.nav.media` in both locales.
    - **Deliberately not built (this slice):** nested folders, drag-and-drop ordering, a "pick from library" picker wired into the blog / appearance / product forms (each still has its own upload), non-PDF documents, an R2 storage backend (still `LocalStorageAdapter`), bulk multi-select move/delete.
  - [x] Forms builder (`/forms`) — done 2026-09-01, Docker-build + module-integration-smoke + live storefront-render verified. `forms` / `form_fields` / `form_submissions` (migration 0044, all ordinary RLS tables); 9 field types (`form_field_type` enum: text/email/phone/textarea/dropdown/radio/checkbox/number/date) — the catalogue is `FORM_FIELD_TYPES` in `src/lib/forms/types.ts` (client-safe, mirrors the enum). `form_fields.options` is one choice per line, text column, only meaningful for dropdown/radio/checkbox; `displayOrder` is a plain typed integer, not drag-and-drop (no sortable-list precedent, same call as Menu Builder). `form_submissions.answers` is a frozen `{label, value}[]` jsonb snapshot per submission (display-only — never filtered/joined, so jsonb over a row-per-answer table; checkbox multi-values are comma-joined) — deleting a form cascades its fields and submissions (the admin delete confirm says so). `src/lib/forms` — `types.ts`, `query.ts` (admin list/get, storefront published-by-slug, submissions), `mutate.ts` (form + field CRUD via `uniqueSlug`, `MAX_FIELDS_PER_FORM` cap), `validate.ts` (server-side required + type re-validation — email regex, `BD_PHONE_PATTERN` reuse, number/date parse, dropdown/radio/checkbox against `options`; honeypot constant). Admin `/forms` (list with field/submission counts) · `/forms/create` · `/forms/[id]/edit` (meta + add/edit/delete fields) · `/forms/[id]/submissions` (newest-first, label:value cards) — all `requirePermission("content:manage")` (Forms is in the Content group, no new permission minted). Storefront `/form/[slug]` (singular — avoids a Next dynamic-segment name clash with admin `/forms/[id]`, same reasoning as `/content` → `/pages`): guest submit action rate-limits by IP (5/10min via `checkRateLimit`), drops a honeypot fill silently, re-validates server-side, records the snapshot; no auto nav/footer link — merchants point a Menu Builder custom-link or a content page at it. Nav entry after `/media`; `admin.forms.*` + top-level storefront `forms.*` (MessageRef-based) + `admin.nav.forms` in both locales.
    - **Deliberately not built (this slice):** CSV export of submissions, email/bell notification on a new submission, a file-upload field type, conditional field logic, reCAPTCHA, multi-page forms, editing/deleting a single submission.
  - [x] Email Gateways (`/email-gateways`) — done 2026-09-01, Docker-build + real-network SMTP-send verified (a live Ethereal test account, not just mocked). One technical adapter, `SmtpEmailAdapter` (`src/lib/email/smtp.ts`, via `nodemailer`, the only file that imports it), serves every provider label — custom SMTP, SendGrid, Mailgun, Amazon SES — because all four are SMTP-shaped (host+port+username+password); `email_provider` enum (`smtp/sendgrid/mailgun/ses/log`) is the merchant-facing *label* driving host/port presets (`EMAIL_PROVIDER_PRESET`) and which encrypted credential slot is used, not a separate class per value (CLAUDE.md rule #5 read as "one implementation per wire protocol" here, same as SMS's `bulksmsbd`+`log`). `log` mirrors the SMS dev fallback. `store_email_settings` (migration 0045, RLS, one row/store — provider/fromName/fromEmail/host/port/secure plain, `secrets` AES-256-GCM per-provider blob exactly like `store_sms_settings`) + `email_messages` (RLS outbox log, mirrors `sms_messages` minus an `event` column). `src/lib/email` — `types.ts`, `providers.ts` (client-safe catalogue + presets), `adapter.ts`/`smtp.ts`/`log.ts`/`index.ts` (the switch point), `settings.ts` (get/save, mirrors `sms/settings.ts`), `send-test.ts` (the only sender in this slice — logs every attempt, success or failure, to `email_messages`). Admin `/email-gateways` — new `email_settings:manage` permission (own Integrations screen, not folded into an existing one), provider picker with JS host/port/secure preset-fill, per-provider credential fieldsets ("•••• saved" pattern), a "Send a test email" action once a provider is saved, recent-sends log. Nav after `/sms-settings`; `admin.emailGateways.*` + `admin.nav.emailGateways` + `admin.roles.permission.emailSettings` in both locales. Verified: settings save/read round-trip (secrets never reach the client), the `log` path, a real SMTP send + real failure (unreachable host) against a live Ethereal SMTP test account, `EmailNotConfiguredError` on incomplete settings.
    - **Deliberately not built (this slice):** no feature calls `sendTestEmail`'s underlying adapter automatically yet — there is no order/customer email notification anywhere in the app (unlike SMS's `sendOrderSms`); this is settings-plus-manual-test only, same framing as `SITE_STRUCTURE.md`'s "Connect a transactional email provider." Also not built: HTML email bodies (text-only), attachments, a provider-side webhook/bounce handler, SendGrid/Mailgun/SES-specific API integrations (deliberately SMTP-only, see above).
    - **Not yet exercised for real:** no live SendGrid/Mailgun/SES/production-SMTP account has sent through this — only a disposable Ethereal test account (real SMTP protocol, not a real inbox). Run one real send per provider you actually use before relying on it.
- **Phase 2+ — Themes marketplace**: full multi-theme browse/purchase/switch — distinct from and beyond the Phase 1 "ship one clean template" decision.
- [x] **Phase 3 — Fraud Checker** (`/fraud-checker`) — done 2026-09-01, Docker-build + module-integration-smoke verified. **No migration** — reads the existing `orders.fraud_*` columns (migration 0023) and the same `createFraudCheckAdapter()` (`src/lib/fraud`, BDCourier or offline stub) the order flow already uses. `src/lib/fraud/history.ts` — `listFraudChecks(storeId, {query, risk, limit, offset})` (COD orders with `fraud_checked_at` set, newest first, `order_code`/`customer_phone` substring + risk-level filters, quota-locked excluded, offset pagination) and `adHocFraudCheck(phone)` (a transient one-off `check()` for a phone that isn't an order yet — **not persisted**, no table to hang it off). `normalizeBdPhone()` accepts `+880…`/`880…`/local with spaces/dashes. The verdict-line parser moved from the order-detail page into `src/lib/fraud/types.ts` as `parseFraudVerdict()` (one copy, both callers). Admin `/fraud-checker` — `requireStaffSession()` (fraud is order-adjacent, open to every staff role like `/orders`, no dedicated permission, no `NAV_PERMISSION` entry), a phone-lookup box (client, inline risk pill) + a filterable/paginated history table linking each row to its order. Nav entry after `/abandoned-checkouts`; `admin.fraudChecker.*` + `admin.nav.fraudChecker` in both locales, reusing the existing `admin.orders.fraud*` risk/advice keys.
  - **Deliberately not built (this slice):** persisting ad-hoc (non-order) lookups, a CSV export, bulk re-check, changing how/when the automatic order fraud check runs. Still stub-only until `BDCOURIER_API_KEY` is set (unchanged from Phase 3).
- **Phase 6 — Apps/Ecosystem**:
  - [ ] App Store (`/app-store`) — public browse/install of feature modules (today: only Installed Apps exists, nothing to browse/install from inside the product)
  - [x] URL Redirects (`/redirects`) — done 2026-09-01, Docker-build + live proxy-redirect HTTP test verified. `redirects` table (migration 0046, ordinary RLS) — per-store `from_path` (exact normalised pathname, unique per store) → `to_target` (site-relative path or absolute `https://` URL) + `status_code` 301/302 + `active`. `src/lib/redirects` — `normalize.ts` (client-safe: `normalizePath` = one leading slash / no trailing slash / no query-string, `canonicalTarget`, `REDIRECT_STATUS_CODES`), `lookup.ts` (`findRedirect(storeId, pathname)` — one indexed `withStoreContext` point-lookup; a Redis redirect-map cache is the noted optimisation if the per-request cost ever matters), `manage.ts` (CRUD + dup-path / self-loop / root-source guards). `proxy.ts` consults it after host resolution, **GET/HEAD only** (301/302 would turn a POST into a GET), resolving a relative target against `resolution.canonicalHost`. Admin `/redirects` — `requirePermission("settings:manage")` (storefront config, like Appearance / Menu Builder — no new permission), add form + per-row edit + two-step delete. Nav after `/default-pages`; `admin.redirects.*` + `admin.nav.redirects` in both locales. Verified live on `demo.localhost`: 301 relative → canonical-host URL, 302, absolute-URL target, inactive row ignored, POST not redirected, trailing-slash source still matched (Next's own trailing-slash 308 fires first, then the redirect).
    - **Deliberately not built (this slice):** wildcard / prefix / regex matching (exact path only), query-string matching, redirect on non-GET, hit counters, bulk import/export.
  - [ ] Order Flow visual editor
  - [x] Product Reviews moderation (`/reviews`) — done 2026-09-02, Docker-build + module-integration-smoke + live storefront/PDP HTTP verified. `product_reviews` table (migration 0047, ordinary RLS) — `product_id` → cascade, `rating` 1–5 (bounded in `src/lib/reviews/types.ts`, not a DB constraint), `author_name`, optional `body`, `status` `review_status` enum (`pending`/`approved`/`rejected`, default `pending`). `src/lib/reviews` — `types.ts` (client-safe: statuses, `RATING_MIN/MAX`, `isValidRating`, body/name caps), `query.ts` (`getApprovedReviews`, `getRatingSummaries` — `{count, average}` per product from **approved only**, `listReviewsForAdmin`, `countReviewsByStatus`), `mutate.ts` (`submitReview` validates + confirms the product is active + inserts `pending`; `setReviewStatus`; `deleteReview`). Storefront: PDP (`(storefront)/product/[slug]`) gained a `#reviews` section — approved list + `getRatingSummaries` header + `<ReviewForm>` (guest, star radios, honeypot, IP rate-limit 5/10min via `checkRateLimit`, `submitReviewAction` in the route's `actions.ts`); a shared pure `<Stars>` component (`src/components/stars.tsx`, server+client safe); `ProductCard` (`storefront-chrome.tsx`) gained an optional `rating` prop, wired through the category page and `src/lib/products/search.ts` — never a fabricated count (SITE_STRUCTURE.md's "50+ reviews shown 4" warning), absent when `count === 0`. **The homepage product cards (`src/app/page.tsx`) were deliberately left untouched** — that file was being edited concurrently by the Public Marketing Site workstream; adding ratings there is a trivial follow-up. Admin `/reviews` — `requirePermission("content:manage")` (user-generated content, like Blog / Media / Forms — no new permission), Pending/Approved/Rejected/All tabs with counts, per-row Approve / Reject / two-step Delete; each action also `revalidatePath("/", "layout")` so the storefront aggregate refreshes. Nav after `/forms`; `admin.reviews.*` + top-level storefront `reviews.*` + `admin.nav.reviews` in both locales.
    - **Deliberately not built (this slice):** verified-purchase badge (no order↔review link), merchant replies, author-side edit/delete, a star filter on the PDP, per-product "reviews off" toggle, photo reviews, helpful-votes, ratings on the homepage cards (see above).
- **No fixed phase — Public Marketing Site** (`SITE_STRUCTURE.md` Part A: the platform's own homepage/pricing/features/testimonials/blog/about/signup). Decoupled from the commerce engine; built in parallel slices alongside Phase 6.
  - [x] **Slice 1 — marketing shell + homepage + pricing + features** — done 2026-09-01, host `next build` + `docker compose build app` + live `next start` smoke (bn default + `en` cookie switch; every price/limit on the pricing cards matched `src/lib/billing/plans.ts`; `/`, `/pricing`, `/features`, `/signup` → 200, unknown path → 404). **No migration; `en.ts`/`bn.ts` untouched** — built concurrently with the Fraud Checker + URL Redirects tabs, so marketing copy is its own isolated i18n module (`src/lib/i18n/messages/marketing.ts` + `getMarketingTranslator()`, which merges a `marketing.*` namespace over the shared messages so client components like the pricing toggle still resolve `billing.plan.*`). New `(marketing)` route group is served only on the platform host — a `getCurrentStore()` → `notFound()` guard in `(marketing)/layout.tsx` keeps it off every merchant domain. The homepage (`/`) stays in `src/app/page.tsx` with a runtime branch (store → storefront, no store → marketing) rendering shared chrome components (`src/components/marketing/chrome.tsx`), the same split the storefront chrome uses; `/features`, `/pricing`, `/signup` live in the route group. `src/lib/marketing/{constants,features}.ts` — `LAUNCH_MINUTES`, `FEATURE_COUNT` (derived — drives the "N features" claim, rule #4/#8), `RECOMMENDED_PLAN_ID`. `PricingSection` is one client component reused as the homepage teaser and the `/pricing` body, reading all figures from `plans.ts`. Feature showcase is a plain tab layout, not a scroll-jacked carousel (rule #6). Sections deliberately **omitted** rather than faked (rule #8): stats bar, trusted-merchant marquee, testimonials-with-counts, template gallery, mobile-app section. Enterprise page + sales-lead funnel **not built** (the 2026-08-31 cut, re-confirmed with the user).
  - [x] **Slice 2 — self-serve signup** — done 2026-09-01, host `next build` + `pnpm lint` + live `next start` smoke (`/signup` renders both locales, 3 plan cards + prices from `plans.ts`, no key leaks) + a direct `createStoreWithOwner()` run against the Docker DB (happy path → `active` store, `trialing` status, `subscription_plan` = picked plan, `trial_ends_at` = now + `TRIAL_DAYS`, `owner` staff row with bcrypt hash, all in one tx; test rows cleaned up). **No migration.** `src/lib/stores/create.ts` — `createStoreWithOwner()` extracted as the one store-creation path: validates, hashes, inserts store + owner `staff_members` in a single transaction (the orphaned-ownerless-store bug fix is preserved), now also sets a real `trial_ends_at` so `effectivePlanId()` grants the courtesy trial tier (previously admin-created stores were `trialing` with a NULL `trial_ends_at` → stuck on Free limits). Returns a stable `{ code }` on failure; callers own the copy. `(platform)/stores/create/actions.ts` rewritten onto it (English `MESSAGES` map, `redirect("/login")`, and now `requirePlatformAdmin()` guards the action itself, not only the page). New public path: `(marketing)/signup/{page,actions,SignupForm}.tsx` — two-panel layout (radio plan cards ← prices/limits from `plans.ts`, `RECOMMENDED_PLAN_ID` preselected → account + store form), `signUp` action IP-rate-limits (5/10min, `checkRateLimit`), requires the ToS checkbox, calls the shared helper, then `signIn("credentials", { redirectTo: "/dashboard" })`. Errors are `MessageRef`s under `marketing.signup.err.*` (isolated marketing i18n module, still no `en.ts`/`bn.ts` touch). The picked plan is stored in `subscription_plan` for `/billing` to pre-highlight; it does not gate anything (trial grants `TRIAL_PLAN` limits regardless until a real payment). **Not built:** email verification (there is no transactional email in the app — Email Gateways is settings-only), a monthly/yearly toggle on the signup plan cards (cycle is chosen at payment time in `/billing`), captcha.
  - [x] **Slice 3 — About + FAQ pages** — done 2026-09-02, host `next build` (70/70) + `pnpm lint` + live `next start` smoke (`/about` + `/faq` render both locales; the shared FAQ bank shows 8 on the homepage, 6 on `/pricing`, all 12 on `/faq`; no key leaks). **No migration, no DB, `en.ts`/`bn.ts` untouched** — built alongside the Product Reviews tab (which took migration 0047). `(marketing)/about/page.tsx` — mission hero, a 4-card values grid, a founding-story section that names `COMPANY_LEGAL_NAME` (the placeholder operating entity from `src/lib/marketing/constants.ts`). `(marketing)/faq/page.tsx` — reuses the `Faq` section component, which grew a `limit` + `showHeading` prop so one `marketing.home.faq.*` question bank (now 12 Q&As) serves the homepage teaser, the pricing page and the full page without duplicated copy (rule #4). `MARKETING_NAV` gains About; the footer Company column gains About + FAQ. `marketing.about.*` / `marketing.faqPage.*` added to the isolated marketing i18n module.
  - [x] **Slice 4 — Testimonials** — done 2026-09-02, `pnpm lint` + `next build` (72/72) + migration 0048 applied + live `next start` smoke (empty state → no nav/footer link and no homepage section; after publishing a row via `psql` → card + badge render, the draft row stays hidden, the link + homepage "What merchants say" preview appear; unpublish/delete reverse it; both locales; `/platform/testimonials` unauth → 307). `testimonials` table (**migration 0048, platform-level — no `store_id`, no RLS**, same call as `stores`/`platform_invoices`; `GRANT … TO amarshop_app` + a `requirePlatformAdmin()` app-layer gate is the only guard — the migration comment spells out why that's not a rule #2 gap). `src/lib/testimonials/{query,mutate}.ts` — `getPublishedTestimonials(limit?)` / `hasPublishedTestimonials()` / admin list + CRUD. `/platform/testimonials` (page + `TestimonialsManager` + actions) — **English-only operator surface, like `/stores/create`, so `en.ts`/`bn.ts` are untouched**; add form + per-row edit / publish-toggle / two-step delete; every write `revalidatePath("/", "layout")`. Public `(marketing)/testimonials/page.tsx` + `TestimonialGrid` (`src/components/marketing/testimonials.tsx`, quotes shown verbatim, never a count — rule #8) + a homepage preview. `MarketingHeader`/`MarketingFooter` take a `hasTestimonials` flag (from one query in the layout + homepage) so the link and the homepage section only exist once something is published. `marketing.testimonials.*` + `nav.testimonials` + `footer.linkTestimonials` in the isolated marketing i18n module.
  - [ ] **Slice 5+ — platform Blog** (needs a table), real **Terms of Service / Privacy Policy** pages (the signup ToS checkbox references them but there is no page yet — needs company/legal specifics from the user), and **dogfooded GTM/GA4/Meta Pixel on the marketing site itself** (`PROJECT_PLAN.md` "Analytics/tracking": set these up on your own marketing site first — needs env config).

## Conventions

- Tenant/store resolution happens once, in `proxy.ts`, and the resolved store is attached to request context — don't re-derive it ad hoc in individual routes.  
- Courier and payment adapters live in their own module per provider, implementing a shared interface (`CourierAdapter`, `PaymentAdapter`) — see `PROJECT_PLAN.md` Section 5 for the exact shape.  
- Background/async work (invoice PDF generation, fraud-score calculation, cart-recovery SMS) goes through a Redis-backed job queue, not inline in the request path.  
- Prefer Server Components and Server Actions where the data doesn't need client-side interactivity; reach for client components deliberately, not by default.  
- Write migrations, not manual schema edits — every schema change is a migration file, including RLS policy changes.

## Before marking any feature "done"

Run it against the audit's own bug list (`PROJECT_PLAN.md` Section 2): does this page restate a number that lives elsewhere? Does any interactive component depend on fragile scroll-position math? Is any internal string leaking into the UI unformatted? Is any count shown to users actually backed by real data?  
